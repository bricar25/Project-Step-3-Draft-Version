<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Products Profile</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <header>
        <h1>Edit Products Profile</h1>
    </header>

    <main>
        <form id="edit_product" action = "/editProduct/{{data[0].productID}}" method = "POST">
            <label for="id">Product ID</label>
            <input type="text" id="prodictID" name="productID" value="{{data[0].productID}}" readonly>

            <label for="sellerID">Seller ID</label>
            <input type="text" id="sellerID" name="sellerID" value="{{data[0].sellerID}}" readonly>

            <label for="category">category</label>
            <input type="text" id="category" name="category" value="{{data[0].category}}" required>

            <label for="brand">Brand</label>
            <input type="text" id="brand" name="brand" value="{{data[0].brand}}" required>

            <label for="price">Price</label>
            <input type="text" id="price" name="price" value="{{data[0].price}}" required>

            <label for="product_condition">product_condition</label>
            <input type="text" id="product_condition" name="product_condition" value="{{data[0].product_condition}}" required>

            <label for="color">Color</label>
            <input type="text" id="color" name="color" value="{{data[0].color}}" required>


            <button type="submit" value="edit_product" name="edit_product">Save Changes</button>
            <button type="button" class="cancel" onclick="window.location.href='/customers';">Cancel</button>
        </form>
    </main>


</body>
</html>
