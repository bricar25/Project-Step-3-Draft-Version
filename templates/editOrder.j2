<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Order Details</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <header>
        <h1>Edit Order Details</h1>
        <p>Note: can't edit customer. if needed, please create a new order</p>
        
    </header>

    <main>
        <form id="edit_order" action = "/editOrder/{{data[0].orderID}}" method = "POST">
            <label for="orderID">OrderID</label>
            <input type="text" id="orderID" name="orderID" value="{{data[0].orderID}}" readonly>

            <label for="order_date">Order Date</label>
            <input type="date" id="order_date" name="order_date" value="{{data[0].order_date}}" style="padding: 10px; margin: 10px 0;" required>

            <label for="order_status">Shipped?</label>
            <select id="order_status" name="order_status" style="padding: 10px; margin: 10px 0;" required>
                <option value="1" {{ 'selected' if data[0].shipped == 1 else '' }}>Yes</option>
                <option value="0" {{ 'selected' if data[0].shipped == 0 else '' }}>No</option>
            </select>

            <label for="order_total">Order Total</label>
            <input type="number" id="order_total" name="order_total" value="{{data[0].total}}" step = "0.01" required>

            <button type="submit" value="edit_order" name="edit_order">Save Changes</button>
            <button type="button" class="cancel" onclick="window.location.href='/viewOrders';">Cancel</button>
        </form>
    </main>


</body>
</html>
