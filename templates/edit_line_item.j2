<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Order Details</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <header>
        <h1>Edit Line Item</h1>
        <p>Notes:</p>
        
    </header>

    <main>
        <form id="edit_line_item" action = "/edit_line_item/{{orderID}}/{{data[0].lineitemID}}" method = "POST">
            <label for="id">Line Item ID</label>
            <input type="text" id="id" name="id" value="{{data[0].lineitemID}}" readonly>

            <label for="product">ProductID</label>
            <input type="text" id="product" name="product" value="{{data[0].productID}}" readonly>

            <label for="line_item_sale_price">Line Item Sale Price</label>
            <input type="number" id="line_item_sale_price" name="line_item_sell_price" value="{{data[0].sell_price}}" step = "0.01" required>

            <label for="line_item_qty" style="padding-top: 30px;">QTY</label>
            <input type="number" id="line_item_qty" name="line_item_qty" value="{{data[0].qty}}" step = "1" required>

            <button type="submit" value="edit_line_item" name="edit_line_item">Save Changes</button>
            <button type="button" class="cancel" onclick="window.location.href='/viewOrderDetails/{{orderID}}';">Cancel</button>
        </form>
    </main>


</body>
</html>
